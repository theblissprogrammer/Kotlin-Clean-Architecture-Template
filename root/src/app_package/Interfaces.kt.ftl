package ${packageName}.common

import com.jiffyondemand.user.commons.AppDisplayable
import com.jiffyondemand.user.commons.AppRoutable

interface ${className}Displayable: AppDisplayable {
}

interface ${className}BusinessLogic {
}

interface ${className}Presentable {
}

interface ${className}Routable: AppRoutable {
}